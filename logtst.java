import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class logtst {
    // private static Logger logger = LogManager.getLogger(logtst.class.getName());

    public static void main(String[] args) {
        Logger logger = LogManager.getLogger(logtst.class.getName());

                        logger.debug("myServer::serverSocket Closed");
                        logger.info("eot find and server is closed");
                        logger.debug("myServer::serverSocket Closed");
                        logger.info("eot find and server is closed");
                        logger.debug("myServer::serverSocket Closed");
                        logger.info("eot find and server is closed");
                        logger.debug("myServer::serverSocket Closed");
                        logger.info("eot find and server is closed");
                        logger.debug("myServer::serverSocket Closed");
                        logger.error("kkkkkkkkkkkkkkkkkkkkkkkkk");
        
    }
    
}
